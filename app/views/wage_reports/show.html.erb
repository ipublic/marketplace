<div class="card">

  <div class="card-header" id= "card-header">Report Span - <%=@report.timespans.first.title%></div>
  <div class="card-header" id= "card-header">Report Span - <%=@report.status%></div>

    <div class="card-body">
      <div class="card">
      <table class="table table-striped">
      <thead>
        <tr>
          <th scope="col" class="card-header"id= "card-header">Time Span</div>
          <th scope="col" class="card-header"id= "card-header">Total Wages</div>
          <th scope="col" class="card-header"id= "card-header">Excess Wages</div>
          <th scope="col" class="card-header"id= "card-header">Taxable Wages</div>

        </tr>
              <tbody>
                <tr>
                  <th scope="row"><%=@report.timespans.first.title%></th>
                  <td><%=@report.status%></td>
                  <td><%=@report.submission_kind%></td>
                </tr>
              </tbody>

            </table>
          </div>
      </div>
</div>